<%= simple_form_for(@tab) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :task %>
    <%= f.association :phase %>
    <%= f.input :order %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>

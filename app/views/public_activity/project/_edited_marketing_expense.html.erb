<%= link_to image_tag(activity.owner.profile.avatar(:thumb), size: "40x40"), activity.owner.profile_page unless activity.owner.nil? || activity.owner.profile.nil? %>
<%= link_to activity.owner.name, activity.owner.profile_page unless activity.owner.nil? || activity.owner.profile.nil? %>

<% marketing_expense = MarketingExpense.find_by_id(activity.parameters[:object_id ]) %>
<% unless marketing_expense.nil? %>
  <%= "#{activity.parameters[:text]} #{link_to("Marketing Expense", marketing_expense)}".html_safe %>
<% else %>
  <%= "#{activity.parameters[:text]} Marketing Expense" %>
<% end %>
on <%= activity.created_at.strftime("%Y/%m/%d %H:%M:%S") %>
<br />

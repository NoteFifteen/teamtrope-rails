<%= form_for(@project_type) do |f| %>
  <% if @project_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project_type.errors.count, "error") %> prohibited this project_type from being saved:</h2>

      <ul>
      <% @project_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :team_total_percent %><br>
    <%= f.text_field :team_total_percent %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= form_for(@task_prerequisite_field) do |f| %>
  <% if @task_prerequisite_field.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task_prerequisite_field.errors.count, "error") %> prohibited this task_prerequisite_field from being saved:</h2>

      <ul>
      <% @task_prerequisite_field.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :workflow_step_id %><br>
    <%= f.text_field :workflow_step_id %>
  </div>
  <div class="field">
    <%= f.label :field_name %><br>
    <%= f.text_field :field_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

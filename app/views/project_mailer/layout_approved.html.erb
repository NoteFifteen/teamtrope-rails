<p>Thank you for approving the sample layout, the production team will review your input and make any changes necessary in the final book layout.</p>
<br>
<p>- Booktrope</p>

<p>Approved by: <%= @current_user.name %></p>
<p>Layout Approval Date: <%= @project.layout.layout_approved_date %> </p>

<% if(@project.layout.layout_approved == 'approved_revisions') && ! @project.layout.layout_approval_issue_list.nil? && @project.layout.layout_approval_issue_list.count > 0 %>
    <%= render 'layout_approval_list_table' %>
<% else %>
    <p>Layout approved with no changes requested.</p>
<% end %>
<br>
